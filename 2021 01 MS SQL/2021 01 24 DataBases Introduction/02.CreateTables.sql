CREATE DATABASE Minions

CREATE TABLE Minions
(
	Id INT PRIMARY KEY NOT NULL,
	[Name] VARCHAR(30) NOT NULL,
	Age TINYINT 
)

CREATE TABLE Towns
(
	Id INT PRIMARY KEY NOT NULL,
	[Name] VARCHAR(30) NOT NULL
)
